#include <stdio.h>
#include <stdlib.h>
#include <algorithm>
int N, NUMS[500][3];

struct data{
    int n,s,e;
    bool operator
};

int main()
{

    int i, j , k;
    scanf("%d", &N);
    for( i=0; i<N; i++){
        scanf("%d %d %d", &NUMS[i][0], &NUMS[i][1], &NUMS[i][2]);

    }
    return 0;
}
